# Dyno The 2nd v1.3
Upload `index.js` and `package.json` to GitHub, replacing the old files. Railway will redeploy.

Receptionist: direct @Dyno only; ignores replies, role mentions, @here and @everyone. Automatic school schedule uses America/Toronto: Mon/Tue/Thu/Fri 9:15–4:30; Wed 9:45–4:30.

Music: `/play`, `/queue`, `/stopmusic`, plus pause/resume, skip, loop and stop buttons. This build uses YouTube search/playback and does not require a Spotify token.

Keep `DISCORD_TOKEN` only in Railway Variables.
